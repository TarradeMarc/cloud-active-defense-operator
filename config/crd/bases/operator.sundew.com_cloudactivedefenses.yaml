---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: cloudactivedefenses.operator.sundew.com
spec:
  group: operator.sundew.com
  names:
    kind: CloudActiveDefense
    listKind: CloudActiveDefenseList
    plural: cloudactivedefenses
    singular: cloudactivedefense
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudActiveDefense is the Schema for the cloudactivedefenses
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CloudActiveDefense
            properties:
              controlpanelAPI:
                description: controlpanelAPI configures the controlpanel API component
                properties:
                  deploymentManagerDBPassword:
                    description: |-
                      deploymentManagerDBPassword is the password for the deployment manager database user;
                      if empty, a random password is generated
                    type: string
                  image:
                    default: ghcr.io/sap/controlpanel-api:latest
                    description: image is the container image for the controlpanel
                      API
                    type: string
                type: object
              controlpanelFront:
                description: controlpanelFront configures the controlpanel frontend
                  component
                properties:
                  image:
                    default: ghcr.io/sap/controlpanel-frontend:latest
                    description: image is the container image for the controlpanel
                      frontend
                    type: string
                type: object
              database:
                description: database configures the shared controlpanel database
                  settings
                properties:
                  password:
                    description: password is the database password; if empty, a random
                      password is generated
                    type: string
                  port:
                    default: 5432
                    description: port is the database port
                    format: int32
                    type: integer
                  user:
                    description: user is the database username; if empty, a random
                      username is generated
                    type: string
                type: object
              deploymentManager:
                description: deploymentManager configures the deployment manager component
                properties:
                  image:
                    default: ghcr.io/sap/deployment-manager:latest
                    description: image is the container image for the deployment manager
                    type: string
                type: object
              domain:
                description: |-
                  domain is the base domain for external access (e.g. "c-28e44bf.kyma.ondemand.com").
                  If not specified, the operator will attempt to auto-detect it from the Kyma cluster configuration.
                type: string
              keycloak:
                description: keycloak configures the keycloak authentication component
                properties:
                  adminPassword:
                    description: adminPassword is the Keycloak bootstrap admin password;
                      if empty, a random password is generated
                    type: string
                  adminUsername:
                    description: adminUsername is the Keycloak bootstrap admin username;
                      if empty, a random username is generated
                    type: string
                  database:
                    description: database configures keycloak's dedicated PostgreSQL
                      database
                    properties:
                      password:
                        description: password is the database password; if empty,
                          a random password is generated
                        type: string
                      port:
                        default: 5432
                        description: port is the database port
                        format: int32
                        type: integer
                      user:
                        description: user is the database username; if empty, a random
                          username is generated
                        type: string
                    type: object
                  image:
                    default: ghcr.io/sap/keycloak-controlpanel:latest
                    description: image is the container image for Keycloak
                    type: string
                type: object
            type: object
          status:
            description: status defines the observed state of CloudActiveDefense
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the CloudActiveDefense resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              resolvedDomain:
                description: |-
                  resolvedDomain is the actual domain being used by the operator.
                  This will be either the user-provided domain or the auto-detected cluster domain.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
